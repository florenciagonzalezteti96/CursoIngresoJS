/*function mostrar()
{
	alert('iteración while');
/*toda iteracion tiene un comienzo, 
un recorrido y una accion que marca su final*
inicializacion, comparacion y cambio de valores.

//si falla el cambio de valores, puede tildarse la maquina
//si fallan la comparacion o la inicializacion no se realiza el WHILE

/*var contador; //1.asigno una variable a verificar
contador=0;
//2.tengo que asegurarme que la condicion sea verdadera.
while(contador<10)//numero de repeticiones)//
{
	contador=contador+1;
	console.log(contador);//para chequear.
}
//el while hace una redireccion y una operacion con c/vuelta
//con cada vuelta el while esta usando el microprocesador.
//el numero que queda afuera del while es el 10,
//porque si no entro en el while, el numero NO es menor a 10.
para que reste el while
var contador;
contador=10;

while(contador>0)//-->el numero de la condicion es el numero con el que 
//va a terminar afuera el while.
{
	contador=contador-1;
	console.log(contador);
}

/*
se utiliza para validacion de datos. cuando vuelve a pedir el dato. 
*/


}//FIN DE LA FUNCIÓN*/

function mostrar()
{
	var contador;
	contador=0

	while(contador<10)
	{
		contador=contador+1;//operacion ANTES DEL ALERT.
		alert(contador);
	}


}//FIN DE LA FUNCIÓN
